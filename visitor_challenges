#1/bin/bash

function xyz {
	while read -a line ; do
		#echo ${line[1]}
		search "visit id: ${line[1]}" "$1"/challenges/*.chg 
	done 
}

visitor_for_room "$1" "$2" | xyz "$1" "$3"


#if [[ -d "$1" ]]; then
#	from="$1"/visitors.txt			
#else
#	echo "No such room!"	
#fi
