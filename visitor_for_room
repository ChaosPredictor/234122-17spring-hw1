#1/bin/bash

a="their is"
b="not"
if [[ -d "$1" ]]; then
	from="$1"/visitors.txt			
	while read line ; do
		if [[ $line = *"$2" ]]; then
			echo $line | cut -d" " -f1,2
		fi
	done < $from | sort -nk2
else
	echo "No such room!"	
fi
