#1/bin/bash

a="their is"
b="not"
if [[ -d "$1" ]]; then
	from="$1"/visitors.txt			
	counter=0;
	while read line ; do
		let counter++		
		echo $line
		if (( counter >= "$2" )); then
			exit
		fi		
	done < $from | sort -nrk2 | sort -sk3 | uniq -2 | sort -nrk2 | cut -d" " -f3
else
	echo $b			
fi
